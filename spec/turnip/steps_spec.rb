require "spec_helper"

RSpec.describe Turnip::Steps do
  it "has a version number" do
    expect(Turnip::Steps::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
